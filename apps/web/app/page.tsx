import ClientEditor from '@/components/Blocknote';

export default function Page() {
    // Creates a new editor instance.
    // Renders the editor instance using a React component.
    return <ClientEditor />;
}
